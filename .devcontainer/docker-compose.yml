services:
  app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
      args:
        CODEX: "true"
    entrypoint: sleep infinity
    environment:
      - CODEX_HOME=/app/.codex
    volumes:
      - ..:/app
